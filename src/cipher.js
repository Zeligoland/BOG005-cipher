const cipher.encode = {
  var test = 'A'
(test.charCodeAt() - 65 + 3) % 26 + 65 // Formula para obtener el codigo ASCII de la letra codificada con el offset ingresado  
  
  ('A'.charCodeAt() - 65 + n) % 26 + 65 // X = número de la letra ASCII obtenida con charCodeAt + n(Offset) 
  
};

const cipher.decode = {


}

export default cipher;
